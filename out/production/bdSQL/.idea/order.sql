CREATE TABLE orders(
                       "orderId" BIGINT,
                       "customerId" BIGINT,
                       "productId" BIGINT,
                       "orderStatus" VARCHAR(200)

);